# Sri Mart — environment config (copy to .env or export in shell)
# Database (libpq). Defaults match local dev if unset.
PGHOST=localhost
PGPORT=5433
PGDATABASE=sri_mart
PGUSER=postgres
PGPASSWORD=postgres

# HTTP listen port (default 8080)
PORT=8080
